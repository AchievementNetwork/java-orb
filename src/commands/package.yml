description: >
  Create a Docker image

parameters:
  maven-settings:
    description: Location of Maven settings file
    type: string
    default: .circleci/settings.xml
  maven-flags:
    description: Additional flags to pass to Maven
    type: string
    default: -B -q -Ddependency-check.skip=true -DskipTests=true -Dcheckstyle.skip

steps:
  - cache-dependencies
  - run:
      name: Package
      command: |
        mvn -s << parameters.maven-settings >> << parameters.maven-flags >> package dockerfile:build
