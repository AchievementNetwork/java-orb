description: >
  Example of a job that will run Java unit tests with code coverage using Maven

usage:
  version: 2.1

  orbs:
    java: achievementnetwork/java-orb@1.0.0

  jobs:
    maven-test:
      executor: java/default
      steps:
        - java/test
